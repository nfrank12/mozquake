This R program takes quake data that was scraped using the mozenda API from qlstats.net and turns into into analyzable data. 

It specifically extracts my data (odt), but can be done with other players (they would need to scrape their own stats from qlstats.net) in order to have a good data set.
